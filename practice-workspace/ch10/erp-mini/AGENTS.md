# Chapter 10 erp-mini 규칙

- `schema.sql`은 데이터 규칙, `db.py`는 연결, `server.py`는 HTTP 조립 책임만 가진다.
- 주문·상세·원장·이벤트 저장은 하나의 트랜잭션으로 처리한다.
- 실제 비밀번호는 `.env`에만 두고 코드나 결과 보고에 포함하지 않는다.
- DB 검증을 실행하지 못하면 성공으로 추정하지 않고 `Not Run`으로 보고한다.

