# Chapter 5 작업 규칙

- SQL은 Chapter 5 전용 Supabase 프로젝트에서만 실행한다.
- 대상이 운영·공용 프로젝트인지 확신할 수 없으면 실행하지 않고 `Not Run`으로 보고한다.
- 원고의 실습 순서를 바꾸거나 뒤 실습의 정답 DDL을 미리 만들지 않는다.
- `DROP`, `DELETE`, `ALTER` 전에는 대상 테이블과 현재 프로젝트를 다시 확인한다.

