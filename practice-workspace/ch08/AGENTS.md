# Chapter 8 작업 규칙

- `project/`만 Chapter 8의 구현 루트로 사용한다.
- 8A에서 Event와 Change를 먼저 완성하고, 8B에서 Persistence와 Router를 추가한다.
- `database.py`, `models.py`와 공개 환경 예시는 시작 scaffold이므로 책임을 바꾸지 않는다.
- 실제 DB가 없으면 dry-run까지만 실행하고 통합 검증은 `Not Run`으로 보고한다.

